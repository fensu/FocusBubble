# Focus Bubble 开发文档

## 1. 产品定位

Focus Bubble 是一个跨平台桌面专注辅助工具。它在屏幕上创建一个跟随鼠标的清晰区域，区域外逐渐变暗或雾化，让用户在电脑上工作时减少被标签栏、通知、桌面图标、聊天窗口等视觉刺激牵走注意力。

第一阶段目标不是做医学工具，也不直接宣称治疗 ADHD，而是提供一个低摩擦的视觉环境调节器。

## 2. 核心体验

用户启动应用后，会看到一个控制面板和一个全屏透明遮罩。遮罩窗口始终置顶、鼠标穿透，不阻挡用户操作其他软件。

当前 MVP 包含三种模式：

- 气泡模式：以鼠标为中心保留圆形清晰区域，适合网页浏览、资料整理、文件管理。
- 阅读模式：保留横向清晰带，适合长文、PDF、文档阅读。
- 代码模式：保留较窄的行级清晰带，适合代码、日志、表格和终端输出。

可调参数：

- 界面语言
- 清晰半径
- 边缘羽化
- GPU 模糊参数
- 外围暗度
- 跟随平滑
- 阅读带高度
- 代码行高度
- 暂停/运行

## 3. 技术选型

当前项目使用：

- Tauri v2：桌面壳、透明窗口、置顶窗口、鼠标穿透、跨平台打包。
- Rust：创建 overlay 窗口并广播全局鼠标坐标。
- React + TypeScript：控制面板和 overlay 渲染逻辑。
- Canvas 2D：绘制暗化遮罩和清晰区域羽化。
- Windows probe：验证 `WDA_EXCLUDEFROMCAPTURE`、D3D11 device 和 Windows Graphics Capture 是否可用。

选择 Tauri 的原因：

- 比 Electron 更轻，适合常驻后台工具。
- Rust 可以处理全局鼠标、快捷键、多显示器、系统托盘等底层能力。
- 前端 UI 迭代速度快，适合快速验证模式和参数。

## 4. 当前架构

```text
Focus Bubble
├─ main window
│  └─ React 控制面板
│     ├─ 模式切换
│     ├─ 参数滑块
│     └─ 本地状态保存
│
├─ overlay window
│  └─ Canvas 全屏遮罩
│     ├─ 鼠标跟随
│     ├─ 气泡模式
│     ├─ 阅读模式
│     └─ 代码模式
│
└─ Rust backend
   ├─ 创建 overlay 窗口
   ├─ 设置透明、全屏、置顶、鼠标穿透
   └─ 每 16ms 广播全局鼠标坐标
```

## 5. 模糊实现策略

透明桌面窗口中的 `backdrop-filter` 在 Windows WebView 上表现不稳定：可能无法模糊窗口后方的真实桌面内容，并且在鼠标移动到屏幕边缘时会因为动态 mask 重建而闪烁。

当前 fallback 改为纯 Canvas 单层渲染：只负责暗化、清晰区域和羽化，不再绘制斜线雾化纹理。这样不会假装成真实模糊，也避免边缘闪烁。

真正模糊不应该走“截图到 CPU -> Canvas ImageData -> CPU blur -> 上传 GPU”的路线。最终架构应该尽量保持全 GPU pipeline：从平台捕获 API 获取 GPU texture，经过 downsample、blur shader、dim shader、focus mask，再通过透明 overlay 呈现。

更详细的高级方案见 [docs/gpu-blur-architecture.md](docs/gpu-blur-architecture.md)。

这个方向的核心难点不是 blur shader，而是如何稳定拿到 overlay 背后的桌面像素，同时避免捕获到 Focus Bubble 自己。如果捕获链路把 overlay 本身也捕进去，下一帧会产生递归画面。

如果后续要做真正模糊，会引入这些复杂度：

- macOS 需要屏幕录制权限。
- Windows 上受保护窗口、DRM 视频可能无法截取。
- Linux Wayland 对全局截屏和 overlay 限制较多。
- Windows 需要验证 overlay capture exclusion 在不同系统版本和多显示器配置下是否稳定。
- HDR、缩放比例、多显示器刷新率、受保护内容都需要单独处理。

所以当前版本把滑块命名为“GPU 模糊”。在 `CanvasRenderer fallback` 下该参数只作为未来 WindowsGpuRenderer 的配置保留，不再产生假的斜线效果。Windows 原生层已经增加 WGC 首帧、GPU surface、DirectComposition target、composition swapchain、透明 back buffer present 和 DComp commit 探针，但还没有把 capture frame 或 shader 渲染结果提交到 overlay。

平台策略：

| 平台 | MVP | 高级模糊 |
| --- | --- | --- |
| Windows | Canvas 雾化 | Windows Graphics Capture + D3D11/12 shader |
| macOS | Canvas 雾化 | NSVisualEffectView behindWindow 优先 |
| Linux X11 | Canvas 雾化 | compositor-dependent |
| Linux Wayland | Canvas 雾化 | 暂不承诺真模糊 |

## 6. 尺寸控制

清晰区域大小不再使用固定小范围。控制面板会根据当前屏幕尺寸计算上限：

- 气泡模式的清晰半径上限使用屏幕对角线，理论上可以扩大到覆盖整个屏幕。
- 阅读模式和代码模式的高度上限使用屏幕高度。
- 边缘羽化也可以拉到屏幕级别，用于做非常柔和的过渡。

## 7. 下一阶段功能

优先级建议：

1. i18n 完整化：当前已有中文/英文界面字典，后续需要抽离到独立 locale 文件。
2. 系统托盘：关闭主窗口后仍常驻。
3. 全局快捷键：例如 Ctrl+Alt+F 快速开关。
4. 多显示器：每个显示器一个 overlay，坐标按屏幕映射。
5. 应用白名单：视频会议、游戏、全屏播放器自动暂停。
6. 配置持久化：从 localStorage 迁移到 Tauri store 或 Rust 配置文件。
7. 鼠标侧键/触控板手势切换模式。
8. Windows 真模糊 prototype：Windows Graphics Capture + D3D GPU blur + overlay 排除捕获。
9. 专注曲线：根据持续工作时间动态调整暗度和羽化。

## 8. 开发命令

安装依赖：

```bash
npm install
```

Web 预览：

```bash
npm run dev
```

桌面开发模式：

```bash
npm run desktop:dev
```

构建前端：

```bash
npm run build
```

桌面打包：

```bash
npm run desktop:build
```

## 9. 当前限制

- 目前 overlay 使用稳定雾化纹理模拟散焦；它不是基于截屏的强制真模糊。
- 当前 Canvas fallback 不再模拟模糊，只做稳定暗化和焦点 mask。
- i18n 当前内置在 React 文件里，后续应拆分到 `src/locales`。
- Linux Wayland 支持需要单独验证。
- 系统托盘、全局快捷键、白名单尚未实现。
- 多显示器当前需要进一步完善。
- 首次运行桌面版可能被系统权限或安全策略影响，尤其是 macOS 和 Linux。
